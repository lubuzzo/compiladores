program main
  begin
    int a;
	int d;
    float b;

function int teste(float a) begin
	float j;
	j := 10;
	return j;
end

    for (a := 0; a < 2; a := + a 1)
      int c;
      c := 10;
      if (c > 0) then
        c := 20;
      else
        for (d := 0; b > 0; a := + a 1)
          a := + 1 2;
		b := c;
        endfor
      endif
	write(c);
    endfor
	write (a, b);
  end
