program main
  begin

	function int main() begin
		int a;
		int d;
		float b;
		int c;

		for (a := 0; a < 2; a := + a 1)
			c := 10;
			if (c > 0) then
				c := 20;
			else
				for (d := 0; b > 0; a := + a 1)
					a := + 1 2;
					b := c;
				endfor
			endif
			write(c);
		endfor
		return 0;
	end
end
