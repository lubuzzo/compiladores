PROGRAM main
  BEGIN

	INT x;

	FUNCTION INT main() BEGIN
		INT a;
		INT d;
		FLOAT b;

		FOR (a := 0; a < 2; a := a + 1)
			INT c;
			c := 10;
			IF (c > 0) THEN
				c := 20;
			ELSE
				FOR (d := 0; b > 0; a := a + 1)
					FLOAT e;
					a := 1 + 2;
					b := c;
				ENDFOR
			ENDIF
			WRITE(c);
		ENDFOR
		RETURN 0;
	END
END
